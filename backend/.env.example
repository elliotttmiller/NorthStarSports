# ===================================================================
# NorthStar Sports Backend Environment Configuration Blueprint
#
# INSTRUCTIONS:
# 1. Copy this file to `.env.local` for local development.
# 2. Fill in the actual secret values (passwords, keys) in `.env.local`.
# 3. `.env.local` is git-ignored and MUST NOT be committed.
# ===================================================================

# --- Server Configuration ---
NODE_ENV=development
PORT=4000
API_VERSION=v1

# --- CORS (Cross-Origin Resource Sharing) ---
# A comma-separated list of URLs allowed to make requests to this backend.
ALLOWED_ORIGINS="http://localhost:3000,https://nssportsclub.ngrok.app"

# --- Prisma Database Configuration (Supabase) ---
# This URL uses the Supabase Transaction Pooler (port 6543) and is used
# by the application at runtime. It is essential for serverless scalability.
# CRITICAL: Replace [YOUR-PASSWORD] with your real database password in .env.local
DATABASE_URL="postgresql://postgres.jddhwhkwgppgpzozbedc:[YOUR-PASSWORD]@aws-1-us-east-2.pooler.supabase.com:6543/postgres?pgbouncer=true"

# This URL is a direct connection to the database (port 5432) and is ONLY
# used by Prisma's migration engine.
# CRITICAL: Replace [YOUR-PASSWORD] with your real database password in .env.local
DIRECT_URL="postgresql://postgres:[YOUR-PASSWORD]@db.jddhwhkwgppgpzozbedc.supabase.co:5432/postgres"
# --- Logging ---
LOG_LEVEL=info
