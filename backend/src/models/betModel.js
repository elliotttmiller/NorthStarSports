// Bet model (stub for type/shape)
export default class Bet {
  constructor({ id, userId, amount, type, legs }) {
    this.id = id;
    this.userId = userId;
    this.amount = amount;
    this.type = type;
    this.legs = legs;
  }
}
