// Game model (stub for type/shape)
export default class Game {
  constructor({ id, teams, odds, startTime }) {
    this.id = id;
    this.teams = teams;
    this.odds = odds;
    this.startTime = startTime;
  }
}
