import React from 'react'
import { Button } from '@/components/ui/button'

  isOpen: boolean

interface PanelToggleProps {
  isOpen: boolean
  side: 'left' | 'right'
  onToggle: () => void
  className?: string
  isOpen,
  onToggle, 
 

  
    <div 
       
      >
          variant
          onClick={
          tit
          <motion.div
              rotate: isOpen ? (
  
          
        </Button>

        {onTogglePin && (
            initial={{ opacity: 0,
       
            cla
            whileTap={{ s
            <Button
              size="sm"
              className={`h-8 w-8 p-0 rounded-full bg-background/60 backdrop-blur-sm border border-border/30 shadow-sm hover:shadow-md transition-all duration-200 hover:bg-background/80 hover:border-borde
                  ? 'text-accent border-accent/30 bg-accent/10' 
         
            >
                animate
                  scale: [1, 1.1, 1]
              
                  scale: { duration: 0.4, 
           
              </motion.div>
          </motion.div>
      </AnimatePr
  )









































