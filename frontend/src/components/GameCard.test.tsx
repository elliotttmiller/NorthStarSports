import { render, screen, fireEvent } from '@testing-library/react';
import { GameCard } from './GameCard';
// You will need to mock the context/store and other dependencies

describe('GameCard', () => {
  it('renders the team names correctly', () => {
    // ... test implementation
  });

  it('calls the addBet function when a bet button is clicked', () => {
    // ... test implementation
  });
});
