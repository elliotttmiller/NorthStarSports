import { WorkspacePanel } from "@/components/panels/WorkspacePanel";

// This is the content that will be rendered as `children` inside your new persistent layout.
export default function HomePage() {
  return <WorkspacePanel />;
}
